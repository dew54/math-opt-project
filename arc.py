from utils import Utils
class Arc:
    def __init__(self, startNode, endNode, resource, capacity, nodeSet):
        self.length = Utils().distance(p1, p2)
        self.cost = length/resource.speed
        self.resource = None
        self.startNode = startNode
        self.trip = resource.trip #SERVE???
        self.endNode = endNode
        self.capacity = capacity     #If arc is: No flow, finite capacity, infinite capacity
        self.nodeSet = nodeSet
        self.type = None                       # If arc is type: alfa, beta, gamma, delta
        #self.loaded = 


    def populate(self):
        #self.flow
        self.cost
        self.startNode
        self.trip
        self.assignedResource
        self.endNode
        self.capacity       
        self.type     

    def getLength():
        p1 = self.startNode.position
        p2 = self.endNode.position
        return Utils().distance(p1, p2)

    


    
# class selfEvaRoad(Arc):
    # def __init__(self):
#         self.capacity
